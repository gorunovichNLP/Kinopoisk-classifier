`AutoModelForSequenceClassification.from_pretrained("./model")` first reads `config.json` to select and configure the correct Transformers architecture. It then creates that model structure and loads the parameter values from the safetensors weights. The configuration defines the model shape, while the weights provide its learned contents.
